{
  "name": "local-tts-service",
  "version": "1.0.0",
  "description": "Local Azure TTS Proxy Service",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",        // 处理 CORS
    "dotenv": "^16.3.1",     // 加载 .env 文件
    "express": "^4.18.2"     // Node.js Web 框架
    // node-fetch v3+ is ESM only, if needed for older Node or specific fetch features.
    // Modern Node has global fetch.
  }
}